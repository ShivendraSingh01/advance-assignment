# Terraform with Jenkins

This folder is intentionally small. The Jenkinsfile can run `terraform init` and
`terraform plan` when `RUN_TERRAFORM_PLAN=true`.

Add cloud resources here later if your class or Jenkins setup needs them.
